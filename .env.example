# Database configuration
DATABASE_PATH=/app/data/queries.db

# GCP Configuration (optional — only needed for BigQuery analytics in Phase 11)
GCP_PROJECT_ID=resume-api-portfolio

# API Configuration
API_HOST=0.0.0.0
API_PORT=8080